# Datos demo

Ejecuta:

```bash
.venv/bin/lifeos-seed-demo --force
```

Esto genera `demo/lifeos-demo.db` con informacion totalmente ficticia. La base demo es independiente de:

`~/Library/Application Support/LifeOS/lifeos.db`

Los archivos `.db` estan ignorados por Git.
